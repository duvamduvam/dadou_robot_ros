#pycairo libs => sudo apt-get install libcairo2-dev libjpeg-dev libgif-dev
#pygobject => sudo apt install libgirepository1.0-dev
adafruit-circuitpython-led-animation~=2.8.0
adafruit-circuitpython-neopixel~=6.3.11
adafruit-circuitpython-pcf8574~=1.0.8
adafruit-circuitpython-servokit~=1.3.17
# Blinka fournit le module `board` mais PAS le backend GPIO : RPi.GPIO reste requis.
# (le paquet PyPI `board` est un homonyme sans rapport, ne pas le remettre)
adafruit-blinka
RPi.GPIO~=0.7.1 ; platform_machine == "aarch64"

imageio~=2.30.0
#openai~=1.35.12
PyAudio~=0.2.14
#pygobject~=3.46.0
# Audio test dependencies
playsound~=1.2.2
simpleaudio~=1.0.4
# L'épingle setuptools==58.2.0 (contournement Humble) casse sur Python 3.12/Jazzy
# (distutils retiré de la stdlib) : on garde la version de l'image de base.
#SpeechRecognition~=3.10.4
SQLObject~=3.11.0

# Dépendances de dadou_utils_ros (la lib n'a pas de packaging propre) :
filetype
colorlog
psutil
pydub